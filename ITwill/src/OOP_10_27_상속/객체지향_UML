
    
    - 상속 관계 : is a 관계

	자식은 부모이다.
	관리자는 사원이다.
	
	자손 extends 부모
	
	다이어그램 - 클래스를 그림으로 표현함 
	
	클래스
	필드
	메서드
	자식 - 부모  가르킴
		
	- 실체화 관계(Realization) 리얼라이제이션 
	부모가 인터페이스  자식이 상속받아 재정의(오버라이딩) 사용
	
	도형 인터페이스 >> 원 클래스 or 삼각형 클래스 	
	
	클래스 -- 인터페이스 점선으로 
	
	- 연관 && 포함관계 && 직접 연관 관계 : has a 관계
	클래스 안에 필드를 참조변수로 선언하여 사용한다. - 인스턴스 정보 저장
	
	ex) Computer << CPU + Mainboard + Memory
		
	- 집합 연관 관계
	
	>> UML2.0 에서는 연관관계로 표현
	>> 포함 관계의 인스턴스와 생명주기 다른 경우
	
	ex) 컴퓨터 << 프린터 
	
	컴퓨터 -> 메모리 
	컴퓨터 -> 메인보드
	접근함 화살표로 표시 
		
	- 복합 연관 관계
	>> 포함 관계의 인스턴스와 생명주기 같은 경우
	
	ex ) 게임 << 캐릭터
	
	- 의존 관계
	>> 메서드의 파라미터로 선언되어 사용되는 참조변수
	>> 일시적으로 메서드에서 인스턴스를 사용할 경우
	
	ex) TV << 리모콘
	
	
	
	