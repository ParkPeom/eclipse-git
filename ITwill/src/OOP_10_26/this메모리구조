

Static 에 Member 라는 클래스가 저장되어있다.
필드 , 메서드 , 생성자가 바이트 기계어로 존재함
Member 에는 필드 , 메서드 , 생성자 가 static 에 메모리에 로딩된다.

class 라고 부르기 보다 static 에 저장된 클래스는 클래즈 라고 부른다.

new 라는 연산자로 heap메모리에 인스턴스를 생성해줌

멤버의 해시코드가 힙에 저장되어있음

힙메모리에 있는 인스턴스 Member id name email 이 만들어 진다.
각각에 값들이 들어가 있다.  메서드를가지고있지않다

Member member1 = new Member();

해시코드를 stack 에있는 member1에 저장

이 해시코드를 이용해서 Member에 접근할수있다. 그런데 은닉화때문에 직집접근 차단됨

Member 클래스 접근할수있는데 클래즈에 접근해서 메서드를 호출할수 있다. 

this.id 출력시 member1 참조변수로 display() 호출 시 
클래즈에 접근하여 this라는 참조변수 를 통해 id 해시코드를 참조한다.
this로 자기자신의 객체로 힙에 인스턴스를 참조할수있게됨 